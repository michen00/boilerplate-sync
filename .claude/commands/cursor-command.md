---
description: Run a Cursor command by name. Pass the command name as the argument.
---

Read and follow the Cursor command at `.cursor/commands/$ARGUMENTS.md`.

When that command references other files via relative paths, resolve them relative to the command file's directory and read those too.
