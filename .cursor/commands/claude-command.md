---
description: Run a Claude Code command by name. Pass the command name as the argument.
---

Read and follow the Claude Code command at `.claude/commands/$ARGUMENTS.md`.

When that command references other files via relative paths, resolve them relative to the command file's directory and read those too.
